{
  "name": "skillmcp",
  "version": "0.1.0",
  "private": true,
  "description": "Provider- and surface-agnostic skills marketplace",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "server": "npm --prefix server run dev",
    "start": "node server/dist/index.js"
  },
  "license": "MIT",
  "bin": {
    "skillmcp": "server/dist/index.js"
  },
  "files": [
    "server/dist",
    "server/package.json",
    "catalog"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^5.1.0",
    "zod": "^3.25.0"
  },
  "engines": {
    "node": ">=20"
  }
}
