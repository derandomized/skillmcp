## Skill submission

**Skill name:** `<name>`
**Submitted by:** <name / email>

### Checklist
- [ ] `SKILL.md` follows the [Agent Skills spec](https://agentskills.io/specification) (name matches dir, description says *what* and *when*)
- [ ] Entry added to `registry.json`
- [ ] `npm run build && npm run validate` passes and generated files are committed
- [ ] No credentials, no network exfiltration, no instructions that override the host's safety rules
